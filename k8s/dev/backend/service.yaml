apiVersion: v1
kind: Service
metadata:
  name: deep-devops-agent-backend
  namespace: storefront-dev
  labels:
    app.kubernetes.io/name: deep-devops-agent-backend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deep-devops-agent-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
