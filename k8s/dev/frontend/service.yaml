apiVersion: v1
kind: Service
metadata:
  name: deep-devops-agent-frontend
  namespace: storefront-dev1
  labels:
    app.kubernetes.io/name: deep-devops-agent-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: deep-devops-agent-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
