apiVersion: v1
kind: Service
metadata:
  name: deep-devops-agent-frontend
  namespace: deep-devops-agent
  labels:
    app.kubernetes.io/name: deep-devops-agent-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deep-devops-agent-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
