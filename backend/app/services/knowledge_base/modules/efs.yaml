type: efs
title: EFS file system
source: terraform-aws-modules/efs/aws
version: "~> 1.0"
config:
  name: '"{{name}}-{{id}}"'
provides:
  efs_id: "module.{{id}}.id"
  efs_arn: "module.{{id}}.arn"
outputs:
  efs_id: "module.{{id}}.id"
