type: secretsmanager
title: Secrets Manager secret
source: terraform-aws-modules/secrets-manager/aws
version: "~> 1.0"
config:
  name: '"{{name}}-{{id}}"'
  secret_string: '"change-me"'
provides:
  secret_arn: "module.{{id}}.secret_arn"
outputs:
  secret_arn: "module.{{id}}.secret_arn"
