# MODULE KB: SQS queue — registry module.
type: sqs
title: SQS queue
source: terraform-aws-modules/sqs/aws
version: "~> 4.0"
config:
  name: '"{{name}}-{{id}}"'
provides:
  queue_arn: "module.{{id}}.queue_arn"
  queue_url: "module.{{id}}.queue_url"
outputs:
  queue_arn: "module.{{id}}.queue_arn"
