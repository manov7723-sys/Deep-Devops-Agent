import { DesignSystemClient } from "./DesignSystemClient";

export const metadata = {
  title: "Design system · DeepAgent",
  robots: "noindex",
};

export default function DesignPage() {
  return <DesignSystemClient />;
}
